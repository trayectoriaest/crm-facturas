name: Actualizar CRM Facturación EST

on:
  schedule:
    - cron: '0 11 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repositorio
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias Python
        run: pip install openpyxl requests

      - name: Instalar Babel para compilar JSX
        run: |
          mkdir -p /tmp/babel
          npm install --prefix /tmp/babel @babel/core @babel/preset-react

      - name: Generar CRM
        env:
          TENANT_ID:     ${{ secrets.TENANT_ID }}
          CLIENT_ID:     ${{ secrets.CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
        run: python build_crm.py

      - name: Commit y push
        run: |
          git config user.name "CRM Bot"
          git config user.email "bot@trayectoriaest.cl"
          git add index.html
          git diff --staged --quiet || git commit -m "CRM actualizado $(date +'%d/%m/%Y %H:%M') [auto]"
          git push
